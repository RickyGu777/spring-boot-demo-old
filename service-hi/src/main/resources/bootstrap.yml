server:
  port: 8762

spring:
  application:
    name: SERVICE-HI
  cloud:
    config:
      name: ${spring.application.name}
      profile: dev
      uri: 'http://localhost:8763'

eureka:
  client:
    serviceUrl:
      defaultZone: http://demo:8761/eureka/
